server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/petrakos995/mikrouslugeRASSUS
          cloneOnStart: true
          searchPaths: config
logging:
  level:
    org.springframework.cloud.config.server: DEBUG
    
